<script src="https://kit.fontawesome.com/da6e295665.js" crossorigin="anonymous"></script>
	<div class="row">
  	<div class="col-xs-10 col-xs-offset-2">
    	<h2>商品新規登録</h2>
    </div>
  </div>

  <div class="row">
    <div class="col-xs-3 col-xs-offset-2">
    		<%= form_for(@item, url: admins_items_path, method: :post, class: "form-horizontal") do |f| %>
        		<div class="img_field">
        		</div>
  	      	<%= f.attachment_field :image, class: "image" %>
      		</div>

      		<div class="col-xs-5">
        		<%= render "form_item", f: f %>

            <%= f.submit "新規登録", class: "btn btn-primary btn-md pull-right" %>
      		</div>
    		<% end %>
  	</div>
	</div>


